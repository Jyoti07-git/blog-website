// script.js

function submitComment(postNumber) {
    const name = document.getElementById(`name${postNumber}`).value;
    const comment = document.getElementById(`comment${postNumber}`).value;
  
    const commentContainer = document.getElementById(`
  
